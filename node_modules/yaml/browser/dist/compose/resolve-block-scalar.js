��ൾ സുഷുപ്തിയിലേക്ക് പോകുന്നുടാബുകളുടെ പ്രകടനംആരോഗ്യകരംഅനാരോഗ്യകരം - ഉയർന്ന ഉപയോഗംഉയർന്ന മെമ്മറി ഉപയോഗം കണ്ടെത്തിഉയർന്ന CPU ഉപയോഗം കണ്ടെത്തിഉയർന്ന മെമ്മറി, CPU ഉപയോഗം കണ്ടെത്തികാണിച്ചിരിക്കുന്ന പ്രകടന വിവരങ്ങൾ ഉപയോഗപ്രദമല്ലപങ്കിട്ട സുരക്ഷാ വിവരങ്ങൾ ഉപയോഗപ്രദമല്ലഎനിക്ക് ധാരാളം അറിയിപ്പുകൾ ലഭിക്കുന്നുനിങ്ങൾ എന്തുകൊണ്ട് സംതൃപ്തരല്ല എന്ന് ഞങ്ങളോട് പറയാമോ:Microsoft Edge എങ്ങനെയാണ് എന്റെ ഉപകരണത്തിന്റെ CPU-വും മെമ്മറിയും മെച്ചപ്പെട്ടതായി ഉപയോഗിക്കുന്നതെന്ന് ഞാൻ കാണുന്നുഞാൻ ഓൺലൈനിലായിരിക്കുമ്പോൾ എന്നെ സുരക്ഷിതമായി നിലനിർത്താൻ Microsoft Edge എങ്ങനെ സഹായിക്കുന്നുവെന്ന് ഞാൻ ഇഷ്ടപ്പെടുന്നുനിങ്ങൾ തൃപ്തരാണെന്ന് എന്തുകൊണ്ടാണെന്ന് ഞങ്ങളോട് പറയുക:ബ്രൗസർ എസൻഷ്യൽസിൽ സംതൃപ്തനാണോ?RewardsIE പുനപ്രവര്‍ത്തന ഡയലോഗ്ഈ വെബ്സൈറ്റ് Internet Explorer-ൽ പ്രവർത്തിക്കില്ലഈ വെബ്സൈറ്റ് Microsoft Edge-ൽ മികച്ചതായി പ്രവർത്തിക്കുന്നുമികച്ച വേഗതയും പ്രകടന നിലവാരവും സുരക്ഷയും അനുഭവിക്കുന്നതിനായി Microsoft Edge-ൽ തുടരാൻ Microsoft ശുപാർശ്ശ ചെയ്യുന്നു.നിങ്ങൾ ഇപ്പോൾ Microsoft Edge- ൽ ബ്രൗസ് ചെയ്യുന്നുഇത് സംഭവിക്കുമ്പോഴെല്ലാം, Internet Explorer-ൽ നിന്ന് എന്റെ ബ്രൗസിംഗ് ഡാറ്റയും താൽപ്പര്യങ്ങളും കൊണ്ടുവന്ന് തടസപ്പെടുത്തലുകൾ ഒഴിവാക്കുകInPrivate windows അടയ്ക്കുകക്രമീകരണങ്ങൾ ആക്സസ് ചെയ്യുന്നതിന് InPrivate windows അടച്ചിരിക്കണം. അവ ഇപ്പോൾ അടയ്ക്കാൻ നിങ്ങൾ ഇഷടപ്പെടുന്നുണ്ടോ?ക്രെഡൻഷ്യലുകൾ ആവശ്യമാണ്Xbox-ൽ സമന്വയം പ്രാപ്തമാക്കുന്നതിന് നിങ്ങളുടെ PIN നൽകണം.കമാൻഡ് പാലറ്റ് ഐച്ഛികങ്ങൾജാലകം അടയ്ക്കുന്നതിന്, ജാലകത്തിന് പുറത്ത് ക്ലിക്ക് ചെയ്യുക അല്ലെങ്കിൽ ESC കീ തിരഞ്ഞെടുക്കുക.കമാൻഡുകളുടെ ഒരു ലിസ്റ്റ് ലഭിക്കുന്നതിന് "ഇതിനേക്കാള്‍ കൂടുതല്‍" എന്ന ചിഹ്നം നൽകുക അല്ലെങ്കിൽ പോപ്പ്-അപ്പ് ലിസ്റ്റ്ബോക്സിൽ സഹായം കാണിക്കുന്നതിന് "�       break;
        case '+':
            for (let i = chompStart; i < lines.length; ++i)
                value += '\n' + lines[i][0].slice(trimIndent);
            if (value[value.length - 1] !== '\n')
                value += '\n';
            break;
        default:
            value += '\n';
    }
    const end = start + header.length + scalar.source.length;
    return { value, type, comment: header.comment, range: [start, end, end] };
}
function parseBlockScalarHeader({ offset, props }, strict, onError) {
    /* istanbul ignore if should not happen */
    if (props[0].type !== 'block-scalar-header') {
        onError(props[0], 'IMPOSSIBLE', 'Block scalar header not found');
        return null;
    }
    const { source } = props[0];
    const mode = source[0];
    let indent = 0;
    let chomp = '';
    let error = -1;
    for (let i = 1; i < source.length; ++i) {
        const ch = source[i];
        if (!chomp && (ch === '-' || ch === '+'))
            chomp = ch;
        else {
            const n = Number(ch);
            if (!indent && n)
                indent = n;
            else if (error === -1)
                error = offset + i;
        }
    }
    if (error !== -1)
        onError(error, 'UNEXPECTED_TOKEN', `Block scalar header includes extra characters: ${source}`);
    let hasSpace = false;
    let comment = '';
    let length = source.length;
    for (let i = 1; i < props.length; ++i) {
        const token = props[i];
        switch (token.type) {
            case 'space':
                hasSpace = true;
            // fallthrough
            case 'newline':
                length += token.source.length;
                break;
            case 'comment':
                if (strict && !hasSpace) {
                    const message = 'Comments must be separated from other tokens by white space characters';
                    onError(token, 'MISSING_CHAR', message);
                }
                length += token.source.length;
                comment = token.source.substring(1);
                break;
            case 'error':
                onError(token, 'UNEXPECTED_TOKEN', token.message);
                length += token.source.length;
                break;
            /* istanbul ignore next should not happen */
            default: {
                const message = `Unexpected token in block scalar header: ${token.type}`;
                onError(token, 'UNEXPECTED_TOKEN', message);
                const ts = token.source;
                if (ts && typeof ts === 'string')
                    length += ts.length;
            }
        }
    }
    return { mode, indent, chomp, comment, length };
}
/** @returns Array of lines split up as `[indent, content]` */
function splitLines(source) {
    const split = source.split(/\n( *)/);
    const first = split[0];
    const m = first.match(/^( *)/);
    const line0 = m?.[1]
        ? [m[1], first.slice(m[1].length)]
        : ['', first];
    const lines = [line0];
    for (let i = 1; i < split.length; i += 2)
        lines.push([split[i], split[i + 1]]);
    return lines;
}

export { resolveBlockScalar };
